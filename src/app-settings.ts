export const settings = {
  client_id: '<@CLIENT_ID@>',
  client_secret: '<@CLIENT_SECRET@>',
  state_password: '<@STATE_PASSWORD@>',
  bot_token: '<@BOT_TOKEN@>',
  origins: '<@ORIGINS@>'.split(',').map(x => x.trim())
};
